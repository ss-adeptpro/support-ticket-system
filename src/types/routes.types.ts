//key must be a string and its value must be a string
export type StringByString = {
  [key: string]: string;
}


export const routes = {
  HOME: 'home',
  LOGIN:'login',
  REGISTER: 'register',
  DASHBOARD: 'dashboard',
  USERS: 'users',
  TICKETS: 'tickets',
  LOGOUT: 'logout'
}